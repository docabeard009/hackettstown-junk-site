# Redirect all .html URLs to clean URLs (301 = permanent)
/*.html    /:splat    301!

# Internally rewrite clean URLs to serve the .html file (200 = no redirect, just serve)
/*    /:splat.html    200
